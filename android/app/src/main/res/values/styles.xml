<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Fondo y barras del sistema del mismo color que el tema oscuro del
             CRM: sin franjas de otro color arriba (hora/notificaciones) ni
             abajo (gestos), ni destellos blancos durante la carga. En modo
             claro, la web lo cambia en caliente a #f1f3f7 mediante
             StatusBarThemePlugin. -->
        <item name="android:windowBackground">@color/tm_window_background</item>
        <item name="android:statusBarColor">@color/tm_window_background</item>
        <item name="android:navigationBarColor">@color/tm_window_background</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>